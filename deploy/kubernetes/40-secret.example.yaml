# DO NOT commit your real secret. Encrypt with SOPS/age, sealed-secrets, or
# whatever your cluster uses. The keys must match what config.yaml references.
apiVersion: v1
kind: Secret
metadata:
  name: politics-secrets
type: Opaque
stringData:
  LLM_API_KEY: ""
  # FRESHRSS_API_PASSWORD: ""
  # X_POLITICS: "benshapiro,glennbeck"
  # X_GOVERNMENT: "POTUS,WhiteHouse"
